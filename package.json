{
  "name": "dahises-tts",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "dev": "pnpm --filter tts-merkez dev",
    "basla": "pnpm --filter tts-merkez basla",
    "test": "pnpm --filter tts-merkez test",
    "tipler": "pnpm --filter tts-merkez tipler",
    "lint": "pnpm --filter tts-merkez lint",
    "migrasyon": "pnpm --filter tts-merkez migrasyon"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
